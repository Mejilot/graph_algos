#ifndef FUNCS_H
#define FUNCS_H
#include <random>
#include <vector>

namespace newstd {
bool ContainsIn(int vertex, std::vector<int>& vertices);
int RandomIntInDist(size_t dist);
}  // namespace newstd

#endif  // FUNCS_H